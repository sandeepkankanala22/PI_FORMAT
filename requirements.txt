# Core (ForecastAI + Excel Agent)
langgraph>=0.2.0
langchain-core>=0.3.0
boto3>=1.34.0
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.31.0     # web research / credible-source fetching
pdfminer.six>=20221105  # PDF text extraction for uploaded resources + fetched PDF sources
pymupdf>=1.24.0        # PDF text extraction for Product Information auto-fill
python-docx>=1.1.0     # DOCX text extraction for Product Information auto-fill
trafilatura>=2.1.0   # main-content extraction for agent-discovered research URLs (strips nav/ads/boilerplate)
sentence-transformers>=3.0.0  # local, no-API-key semantic re-ranking of fetched content per assumption field
ddgs>=9.0.0           # real-time DuckDuckGo web search, no API key — grounds source discovery in a live search

# ForecastAI backend
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
openai>=1.30.0

# Excel Agent
openpyxl>=3.1.0
colorlog>=6.8.0
mcp>=0.9.0

# PPTX Agent
python-pptx>=1.0.0
matplotlib>=3.7.0

# Optional (Streamlit UI, data)
pandas>=2.0.0
streamlit>=1.35.0
plotly>=5.20.0
